/**
 * Shared constant only — no Node-specific APIs here, so this file is safe to
 * import from Next.js Middleware (Edge runtime) as well as from Route
 * Handlers (Node runtime).
 */
export const ADMIN_SESSION_COOKIE = "adashe_admin_session";
